spring:
  security:
    enabked: fakse
  application:
    name: api-gateway
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true

server:
  port: 8080